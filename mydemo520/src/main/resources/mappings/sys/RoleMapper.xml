<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="org.vie.mydemo520.mapper.sys.RoleMapper">
	
	<!-- 查询 -->
	<select id="get" resultType="Role">
		SELECT r.* FROM sys_role r WHERE
		r.id=#{id}
	</select>
	
	<!-- 根据名称查询 -->
	<select id="getByName" resultType="Role">
		SELECT
			r.*
		FROM
			sys_role r
		WHERE			 
		    r.name=#{name}
	</select>
	
	<!-- 根据用户Id查询角色 -->
	<select id="queryListByUserId" resultType="Role">
		SELECT
			r.*
		FROM
			sys_role r
		LEFT JOIN sys_user_role ur ON ur.role_id = r.id
		WHERE
		AND ur.user_id = #{userId}
	</select>


	<!-- 查询（分页） -->
	<select id="queryRole" resultType="Role">
		SELECT
		r.*
		FROM sys_role r
		<if test="name != null and name != ''">
			AND r.name LIKE CONCAT('%',#{name},'%')
		</if>
	</select>
	
	<!-- 保存 -->
	<insert id="save" useGeneratedKeys="true" keyProperty="id">
		INSERT INTO sys_role (
		id, 
		name,
		description,
		available
		)
		VALUES (
		#{id}, 
		#{name},
		#{description},
		#{available}
		)
	</insert>

	<!-- 更新 -->
	<update id="update">
		UPDATE sys_role
		SET name = #{name},
		description = #{description},
		available=#{available}
		WHERE id = #{id}
	</update>
	
	<!-- 删除 -->
	<update id="delete">
		delete sys_role
		WHERE id IN
		<foreach collection="list" item="id" index="index" open="("
			separator="," close=")">
			#{id}
		</foreach>
	</update>

	<!-- 保存角色与资源的关联关系 -->
	<insert id="saveRoleMenu">
		INSERT INTO sys_role_resource(role_id, resource_id)
		<foreach collection="resources" item="menu" separator=" UNION ALL ">
			SELECT #{id}, #{menu.id} FROM DUAL
		</foreach>
	</insert>

	<!-- 删除角色与资源的关联关系 -->
	<delete id="deleteRoleMenu">
		DELETE FROM sys_role_resource WHERE role_id = #{id}
	</delete>

</mapper>